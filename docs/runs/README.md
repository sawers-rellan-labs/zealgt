# Run cards
One file per run (`<run>.md`), written and approved before anything is submitted:
purpose; selection criteria (taxon, BC1 samples, NILs, total BC1 coverage λ); the named donors with those numbers; explicit exclusions;
inputs, outputs, and what "done" means. Every pipeline entry checks the run card before starting.
